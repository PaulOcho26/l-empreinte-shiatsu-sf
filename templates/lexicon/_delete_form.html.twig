<form method="post" action="{{ path('app_lexicon_delete', {'id': lexicon.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ lexicon.id) }}">
    <button class="btn">Delete</button>
</form>
